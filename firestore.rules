rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all documents
    // Note: For production, you should add proper authentication and authorization rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
